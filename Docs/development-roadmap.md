# Дорожная карта внедрения (пошагово)

Правила: см. `WORKING_PROTOCOL.md`. Спека продукта: `spec-economy-referral-admin.md`.

**Текущий baseline (зафиксировать в Git):** backend `pytest tests/` — 16 passed.

---

## Этап 0 — Инструменты (владелец + агент)

| # | Действие | Готово |
|---|----------|--------|
| 0.1 | Установить Git for Windows, `git init` в `iGaming Project`, первый коммит | ☐ |
| 0.2 | Убедиться, что `.gitignore` исключает `node_modules`, `.next`, `__pycache__`, `.env` | ☐ |

---

## Этап 1 — Заказ → оплата с баланса → генерация (стабильность)

**Цель:** один предсказуемый путь без «лживых» ошибок в UI; генерация уже есть (`/orders/{id}/generate`), LLM в `to_thread`.

| # | Задача | Проверка |
|---|--------|----------|
| 1.1 | Аудит фронта: создание заказа → оплата с баланса → кнопка генерации → отображение статуса/ошибок | Ручной сценарий |
| 1.2 | При необходимости — интеграционные тесты API заказов (mock LLM) | `pytest` |
| 1.3 | Явные сообщения об ошибках (недостаточно средств, не оплачен, LLM недоступен) | UI + API |

**Не трогаем:** рефералку, ЮKassa, скидку 30%, пока этап не закрыт.

---

## Этап 2 — Пополнение баланса (ЮKassa / ЮMoney)

**Сделано (dev):** `POST /api/v1/wallet/mock-topup` при `ENABLE_USER_WALLET_MOCK_TOPUP=true` + блок в кабинете при `NEXT_PUBLIC_ENABLE_WALLET_MOCK=true` (мгновенное зачисление через существующий `apply_payment_succeeded`).

**Сделано (ЮKassa):** при `YOOKASSA_SHOP_ID` + `YOOKASSA_SECRET_KEY` — `POST /wallet/yookassa/create` (редирект), `POST /wallet/yookassa/webhook` (проверка статуса через API + `apply_payment_succeeded`), `GET /wallet/yookassa/status`, блок в кабинете. В личном кабинете ЮKassa указать публичный URL webhook.

| # | Задача | Проверка |
|---|--------|----------|
| 2.1 | Актуализировать кабинет ЮKassa, ключи в `.env` | Документация провайдера |
| 2.2 | ~~Endpoint создания платежа + redirect~~ | Готово |
| 2.3 | ~~Webhook → зачисление~~ | Готово (идемпотентность в settlement) |

---

## Этап 3 — Реферальная привязка и ссылка в ЛК

| # | Задача | Проверка |
|---|--------|----------|
| 3.1 | Cookie `ref` + запись `referrer_user_id` при регистрации | Новый пользователь по ссылке |
| 3.2 | `referral_code`, опционально кастом slug (уникальность) | ЛК показывает ссылку |

---

## Этап 4 — Начисления рефереру 50% / 15%

| # | Задача | Проверка |
|---|--------|----------|
| 4.1 | Флаг «первое пополнение реферала» + проводки в ledger | Суммы по спеке |
| 4.2 | Идемпотентность от `payment_id` | Повтор не начисляет |

---

## Этап 5 — Скидка 30% на первый заказ

| # | Задача | Проверка |
|---|--------|----------|
| 5.1 | Правило в `calculate_order_price` / аналог | Первый заказ дешевле |
| 5.2 | Текст на регистрации/входе | Видно пользователю |

---

## Этап 6 — Админка (расширение)

Список пользователей, карточка, платежи, рефералы, бан, выдача Credits, **last_seen + User-Agent → тип устройства**, без паролей в открытом виде.

---

## Этап 7 — Публичные тексты и позиционирование

Credits, рефералка, скидка; формулировка iGaming + другие темы.

---

## Этап 8 — Домен, хостинг, CI/CD

Без ручной заливки файлов после каждого апдейта — только pipeline из репозитория.

---

*Обновляй таблицы по мере закрытия этапов.*
